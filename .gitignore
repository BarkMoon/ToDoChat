# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.venv/
venv/
env/

# Editors / IDE
.vscode/
.idea/
*.swp
*~

# OS junk
Thumbs.db
Desktop.ini
.DS_Store

# Logs / local runtime output
*.log

# Local per-user app state (project list / current working folder)
projects.json

# Dedicated Chromium profile for the app-window launcher (server.py)
/.browser-profile/

# Conversation-persistence store: per-project memory notes + their backups.
# Backups (trash/) are retained copies of deleted/overwritten notes and are
# NEVER read back by the server, so they never re-enter a prompt.
/.todochat/

# Note: TASKS.md is intentionally tracked (it is the app's task state).
