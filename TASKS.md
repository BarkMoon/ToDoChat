# ToDoChat タスク

## 進行中
- [ ] コンテキスト使用量バーの改善（詳細は着手時に説明）。
## 次の候補
（リモート接続ロードマップ。ステップ①HOST可変化・同一WiFi疎通は「LAN接続をデフォルト化」等で完了済み。②トークン認証も完了。本命の接続経路はTailscale、ポート開放/公開トンネルは非推奨）
- [ ] Tailscale serve 経由アクセスのトークン認証（厳密案）。serve はTLS終端後に `127.0.0.1` からプロキシするため、現状 `_authenticate()` のloopback無条件許可でトークン認証がバイパスされる（＝tailnet参加端末なら誰でも到達可という割り切りで現在は運用）。より厳密にするなら、serve が付与するヘッダ（`Tailscale-User-Login` 等）の有無で「serveプロキシ経由リクエスト」を判定し、その場合はloopback免除を外してトークン（またはTailscaleユーザーID）を要求する。`app/server.py` の `_client_is_loopback()`／`_authenticate()` 周辺。
- [ ]（任意）ネイティブAndroid化＋プッシュ通知（後回しでよい）

## 完了
- [x] 開始時確認メッセージ（挨拶生成）のオーバーヘッド削減。案A: `read_tasks` が TASKS.md の `## 完了` 見出し以降を切り落とし、挨拶プロンプトへ注入するトークンを削減。案B: `init_greeting_stream` の挨拶生成モデルをクライアント選択に依らず Sonnet 固定に（定型の3セクション要約用に軽量化。本番会話は従来どおりユーザー選択モデル）。`app/server.py` の read_tasks / init_greeting_stream 周辺。APP_VERSION 0.8.7→0.8.8
- [x] 記憶マーカー誤検出によるAI返信途切れの修正。原因: 記憶マーカー（`[[TODOCHAT_MEMORY]]`）の判定が「本文中の最初の出現」を無条件にブロック開始と誤認し、以降を丸ごとカットしていた（サーバ側 `MEMORY_RE` ＋クライアント側 `stripMemory` の両方）。そのため、マーカーの書き方を例示・説明する返信（行頭以外での言及）まで途中で切れてしまう不具合があった。対処: 両者を行頭アンカー化（server: `re.MULTILINE` 付き `^[ \t]*`、client: `/^[ \t]*.../m`）して、行頭で始まる場合のみ実際の記憶ブロックとして扱うよう厳密化。加えて streamedTurn の最終描画を `final.reply`（サーバで既に除去済みの完全なテキスト）に統一し、分割（segmented）時は除外するよう修正。実サーバー（稼働中プロセス）へ `/api/chat` で「マーカーを本文中に例示する返信」を実投げしてE2E確認済み（返信が途切れず完全な形で返ることを確認）。APP_VERSION 0.8.9→0.8.10
- [x] 終了時記憶保存のオーバーヘッド削減（案D/E: 論理時刻クロックで鮮度管理）。会話ごとに `LAST_USER_TURN`/`LAST_MEMORY_SAVE` を論理時刻でトラッキングし、`apply_memory_block`／`write_memory`／`remember_stream`／`snapshot_memory_blocking` の各保存経路で `mark_memory_saved`、ユーザー発話開始時に `mark_user_turn` を呼ぶ。`finalize_memory_if_enabled` は `memory_is_fresh`（最後の記憶保存が最後のユーザー発話以降）がTrueなら `snapshot_memory_blocking` を呼ばず即終了し、ターン内 `TODOCHAT_MEMORY` 保存との重複や終了ボタンの待ち（最大120秒）を回避。実機確認済み。APP_VERSION 0.8.8→0.8.9
- [x] PWA全画面化の実機最終確認（案A: Tailscale serve でHTTPS配信）。Tailscale管理コンソールで「HTTPS Certificates」を有効化のうえ、⚙️設定の「🔒 全画面PWA用（Tailscale HTTPS）」に出る `https://desktop-41gnrgo.tail0a7381.ts.net/` をAndroidで開き、ホーム画面追加が全画面（standalone）表示になることを実機で確認済み。
- [x] PWA全画面化のためのTailscale HTTPS配信（案A: `tailscale serve`）。`server.py` に `_ts_dns_name()`（`tailscale status --json` の `Self.DNSName` をUTF-8明示デコードで取得・キャッシュ）、`tailscale_serve_url()`（LANモード時に `https://<MagicDNS名>/` を返す。serveはTLS終端後に127.0.0.1からプロキシ＝loopback信頼でトークン不要のため `?token=` を付けない）、`_ensure_tailscale_serve()`（起動時にLANモードなら別スレッドでベストエフォート実行。`_serve_already_fronts_port()` の読取りチェックで既設定なら即スキップ＝毎起動の再ブロック回避、未設定時のみ `tailscale serve --bg --https=443 http://127.0.0.1:PORT` を時間上限付きで試行）を追加。Tailscale実行ファイル探索を `_TS_EXES` に共通化。`list_projects`/`regenerate_token` の応答と起動時コンソールに `tailscale_serve_url` を追加。`index.html` の⚙️設定に「🔒 全画面PWA用（Tailscale HTTPS）」枠（URL＋QR・トークン不要の注記）を追加、`renderQrBlock()` にラベル引数を追加。実機のMagicDNS名 `desktop-41gnrgo.tail0a7381.ts.net` の検出・serve URL生成・両API応答への反映を検証済み（cp932デコード事故を修正）。※実際のHTTPS疎通＝全画面表示は管理コンソールでHTTPS Certificates有効化後に確認（進行中タスク参照）。トークン認証の厳密化は将来タスクへ。APP_VERSION 0.8.5→0.8.6
- [x] PWA基盤の追加（ホーム画面追加対応）。`app/manifest.webmanifest`（name/short_name/display=standalone/theme・background色/icons）と `app/sw.js`（最小のfetchパススルーSW）、`app/icons/{icon-192,512,180}.png`（stdlibのzlib+structで生成、生成器 `app/gen_icons.py`）を追加。`server.py` の `do_GET` に `/manifest.webmanifest`・`/sw.js`（ルート配信でスコープ確保）・`/icons/*.png` のルートと `_send_static()` を追加（パストラバーサル防御あり）。`index.html` にPWA用meta（theme-color・apple-mobile-web-app-*・manifest・apple-touch-icon）＋`viewport-fit=cover`＋SW登録スクリプトを追加。HTTP経由でE2E検証済み（全ルート正常・PNGシグネチャ/寸法正常・トラバーサル防御OK）。Android実機でホーム画面追加を確認（※全画面化は別タスク＝要HTTPS）。APP_VERSION 0.8.4→0.8.5
- [x] 設定ウィンドウに外出先用（Tailscale）のアドレス＋QRを追加。サーバーが Tailscale IP（`100.64.0.0/10`）を自動検出（`app/server.py` の `_tailscale_ip()`：`tailscale ip -4` を優先、失敗時は自ホストアドレスからCGNAT帯を走査、`_is_cgnat()` で判定、`_TS_IP_CACHE` でメモ化）し、`tailscale_auth_url()` を `list_projects`/`regenerate_token` の応答に `tailscale_url` として追加。⚙️設定の接続トークン欄を「📶 同一Wi-Fi用（LAN）」「🌐 外出先用（Tailscale VPN）」の2枠に分け、それぞれURL＋QRを出し分け（`renderQrBlock()`）。起動時コンソールにも外出先URLを表示。実IP `100.99.133.53` の検出・LAN/Tailscale両URL生成・CGNAT境界判定をバックエンド検証済み。`docs/Tailscale接続.md` を実装済み表記に更新。APP_VERSION 0.8.2→0.8.3
- [x] Tailscale（VPN）で外出先対応。導入手順を `docs/Tailscale接続.md` に文書化（PC/スマホのインストール〜tailnet参加〜トークン付きURLでのログイン〜トラブルシュート）。ToDoChatはLANモード（`0.0.0.0`待受）ならTailscale仮想アダプタでも自動で受かるためサーバー側の改修は不要。実機E2E検証済み（スマホからPCのTailscale IP `100.x` でトークンログイン→会話往復→🔄同期→🔧確認カードの許可→実行まで成功）。フックコールバックは `HOST=127.0.0.1` 固定でPC内ループバックから叩くためTailscale経由でも無傷。Tailscale仮想アダプタはWindows上で「プライベート」分類のため既存FWルール（`profile=private`）がそのまま適用されFW追加設定は不要と実地確認。`docs/LAN接続.md` からも相互リンク。※設定ウィンドウのQR/URLへのTailscale IP自動併記は任意タスクとして別途（現状は外出先接続時にTailscale IPを手動確認）
- [x] 接続トークンのQRコード表示。設定ウィンドウの接続トークン欄に、`?token=` 付きスマホ用ログインURLをエンコードしたQRコードを表示（スマホのカメラで読むだけで初回認証が済む導線）。stdlibのみ制約への対処として、URLは既にクライアント側にあることを活かしサーバー（Python）ではなく `app/index.html` にライブラリ非依存の自前QRエンコーダ（バイトモード・EC=M・バージョン1-10・自動バージョン選択・8マスク評価・SVG出力）を内蔵（CDN不要でLANオフラインでも描画）。`renderAuth()` で `authUrl` がある時のみ描画、長すぎる場合はテキストのフォールバック。JSエンコーダをPythonへ忠実移植し「エンコード→独立デコード（RSシンドローム=0検証・フォーマット情報のBCH復号・バイトモードのペイロード往復一致）」でv1/v4/v5（ブロックインターリーブ含む）・マルチバイトUTF-8まで全ケース検証済み
- [x] トークン認証の追加（遠隔化の必須前提）。方針「ループバック(127.0.0.1)は無条件許可／非ループバックはトークン必須」。①初回起動時に `secrets.token_urlsafe(24)` を生成し `projects.json` に `auth_token` として永続化（再生成API `/api/auth/regenerate` で全Cookie失効）。②`Handler.do_GET`/`do_POST` 冒頭に `_authenticate()` ゲートを挟む。`client_address[0]` が `127.0.0.1`/`::1`/`::ffff:127.0.0.1` なら無条件許可（PC本体の窓＋フックコールバックは `HOST=127.0.0.1` 固定なので改修不要）。非ループバックは Cookie 検証、`?token=` 一致なら `HttpOnly; SameSite=Lax` Cookie をSet-Cookieして302でトークンをURLから除去、未認証はGETにログインページ・APIのPOSTに401。比較は `secrets.compare_digest`。③⚙️設定ウィンドウに接続トークン＋`?token=`付きスマホ用URL表示・🔑再生成ボタンを追加、`list_projects` が `auth_token`/`lan_url` を返す。④起動時コンソールとLAN注記を「認証未実装」から更新。0.0.0.0待受でLAN IP（非ループバック）経由の実HTTP E2E検証済み（loopback免除／未トークン→ログイン／誤トークン→ログイン／正トークン→302+HttpOnly Cookie／Cookie→index／API未認証→401／Cookieで `/api/projects` が auth_token・lan_url を返す）
- [x] 会話再開・区切りコマンド群（/startup /new /save /refresh）と再接続時の自動復元。①アプリ起動・再接続時、`.todochat/transcripts/` に会話が残っていればそれを画面へ復元するだけにし、起動時挨拶（TASKS.md要約・記憶ログ注入・サーバー側セッション開始）は自動実行しない（PCとスマホの合流や単純なアプリ再起動でセッションが勝手にリセットされるのを防止）。履歴が無いフォルダのみ従来どおり自動で挨拶を表示。②`/startup`: 起動時挨拶を手動で再表示。③`/new`: `/clear`相当（履歴・記憶ログ・セッション破棄）に続けて挨拶を表示し、まっさらに始め直す。④`/save`: 終了時記憶と同じ経路で記憶ログへスナップショットを保存したうえで、記憶ノートは残したままフルログ（transcript＋session_id、新API `/api/clear-log` → `clear_log()`）だけを削除（トークン節約目的で次回起動を要約から再開させる）。⑤`/refresh`: `/save`に続けて`/startup`を実行（要約への圧縮＋新しい会話の即時開始）。入力欄プレースホルダーに全コマンドを明記。実機でE2E検証済み
- [x] LAN接続をデフォルト化＋設定ウィンドウで接続モードを切替可能に＋ファイアウォール自動設定。①`start-lan.bat` を `start.bat` にマージし削除（LAN公開をデフォルト化）。②待受アドレスの決定をアプリ内設定へ移行：⚙️設定ウィンドウに「接続モード」を追加し、LANモード（`0.0.0.0`・既定）／ローカル限定（`127.0.0.1`）を選択可能に。`projects.json` に `lan_mode` として永続化し次回起動時に反映（`resolve_bind_host()` が env `TODOCHAT_HOST` の明示指定を最優先、無ければ `lan_mode`）。③設定UIに現在の待受状態・「変更は次回起動時に反映」・env上書き時の無効化・ファイアウォール状態を表示。④切替時にWindowsファイアウォールの受信許可ルール（TCP・`profile=private`・`localport`はポートに追従）を自動同期：LANモードで追加（初回設定）・ローカル限定で削除（クリーンアップ）。既存のUAC昇格パターン（`_netsh_elevated`／`_schtasks_elevated`同型）で切替時に一度だけ昇格し、既に望む状態なら昇格せず何もしない。netsh出力はcp932デコード事故を避けるためDEVNULL（returncodeのみ使用）。UACキャンセルは静かに握り潰し設定フラグは保存（結果は`firewall`としてUIへ）。⑤`docs/LAN接続.md` を設定ウィンドウ方式＋自動ファイアウォールに全面更新。バックエンド（分岐ロジック・`set_lan_mode`永続化・`_firewall_rule_exists`の読取り・非昇格の同一状態分岐）をユニット検証、テスト用ポートでHTTP経由の `/api/lan-mode` をE2E確認済み（昇格が要るadd/deleteはUAC都度確認の実運用パスとして未自動テスト）
- [x] 確認モードの許可待ちを無期限化・ブロック理由の詳細化。①`request_permission` のサーバー側待機から固定タイムアウト（旧300秒）を撤廃し、ユーザーの応答があるかCLIプロセスが終了するまで無期限に待機（プロセスが消えた/run破棄時のみ諦める）。②`guard_settings_arg()` の PreToolUse フック登録に `"timeout": 86400` を明示指定し、CLI側のデフォルトフックタイムアウトでサーバー側の無期限待機が横取りされ勝手に打ち切られる不具合を修正。③フックがBash/Editを拒否した際、どのツール・どのコマンド/パス・どんな理由でブロックされたかを `/api/hook/denial` 経由でサーバーに詳細記録させ、ターン終了時のブロック通知（`showDenials`）にツール名・対象・理由を整形表示するよう強化（旧: ツール名のみの簡素な1行表示）。実機で「Bashコマンドを確認カードのまま5分以上放置→その後に許可」しても正常に待機し続けることをE2E確認済み
- [x] ツール使い勝手向上（改行キー設定・設定ウィンドウ・端末間同期）。①入力欄のEnterキー挙動を切替可能に（既定を「Enterで改行・Ctrl+Enterで送信」に変更し誤送信を防止、旧来の「Enter送信・Shift+Enter改行」も選択可。IME変換確定のEnterでは送信しない。スマホでもEnterで改行。`projects.json` に `enter_to_send` として永続化）。②ヘッダに⚙️設定ウィンドウを追加し、改行キー設定とスタートアップ登録トグルをその中へ移設（他は直置きのまま）。③🔄同期ボタンを追加。サーバーが各ターンの発言を `.todochat/transcripts/` に記録（直近200ターン・`/clear` で消去）し、押下で取得・画面へ再生することでスマホ⇔PCなど別端末間の会話を引き継げるように。バックエンドをユニット検証、PC・スマホ実機でE2E確認済み
- [x] スマホ等のLAN接続対応（待受アドレスの環境変数切替・start-lan.bat・接続ガイド追加）。詳細は `docs/LAN接続.md` を参照
- [x] 許可確認カードにコマンドの意図・処理内容を併記。confirm モードで手動許可を求める Bash コマンドについて、CLIが各コマンドに付す `description`（意図・処理内容の平易な説明）を許可カードに表示。サーバは非自動許可の perm_request にのみ `desc` を載せ、UIは `.perm-desc`（意図・処理内容: …）としてコマンド行の上に併記。自動許可（安全コマンド）カードには付けない。実CLIでE2E確認済み
- [x] 使用モデル記録メモ（設計・実装を行ったモデルをコミット時に参照）。プロジェクトごとに `.todochat/models/` へ貢献モデルの表示名リスト（Opus 4.8 / Sonnet 5 / Haiku 4.5）を保存。ターン中に Edit/Write 等のファイル編集ツールを実行したモデルのみ自動記録（助言・確認のみの読み取り専用ターンは記録しない）。チャットの各ターンに未コミットの貢献モデルを1行注入し、AIが git commit 時にコミットメッセージ末尾の (モデル名) へ反映。`git commit` の許可（allow）を検知したらメモをクリア。ヘッダの🏷️ボタンから閲覧/編集/削除するモーダルUIを追加（記憶ログUIと同型）。バックエンドをユニット検証、実CLIで「編集ターンでの記録」「注入がAI文脈へ到達」「commit許可でのクリア」をE2E検証済み
- [x] Windowsスタートアップへの登録（タスクスケジューラ方式・ログオン時トリガー）。ヘッダのトグル `🚀 スタートアップ登録` で、現在の登録状況を起動時に `schtasks /query` で取得して表示し、ボタンで登録/解除を切替。タスクは `start.bat` を `/sc onlogon /rl limited`（標準ユーザー権限）で実行。作成/削除には管理者権限が必要なため、そのクリック時のみ PowerShell `Start-Process -Verb RunAs` で一度だけUAC昇格（日々の自動起動は非昇格）。UACキャンセルは cancelled として静かに握り潰し状態は不変。実機で登録→状態確認→解除→状態確認のE2E検証済み
- [x] 使用感の評価（良くなければUIをTauri等へ変更検討）。モデル選択（Opus/Sonnet/Haiku）の切替動作を含め確認し、現状の作りに満足のため完了とする
- [x] フルログ復元モードの任意選択（深掘り作業時だけ前回の会話ログ全体を復元）。session_id を `.todochat/sessions.json` に毎ターン永続化（トグルとは独立）。グローバルトグル `📜 フルログ復元`（projects.json に永続化・デフォルトOFF・前回選択を維持）がONの時、起動時の第一声を要約メモではなく `--resume` で会話全体を復元して再開（軽い継続プロンプトでTASKS.md/記憶メモの二重注入を回避）。無効な session_id では通常起動へ自動フォールバック＋UI通知。`/clear`・プロジェクト削除で sessions.json も後始末。バックエンドをユニット検証（永続化/トグル/起動分岐/フォールバック）、実CLIでE2E検証（会話→サーバー再起動→ON起動で合言葉「キンモクセイ2026」が文脈保持され正答／複数回のアプリ再起動を挟んだ2問連続の保留出題→正答で文脈保持を再確認）済み
- [x] アーキテクチャ方針の決定（Pattern A: claude CLIラッパー）
- [x] ヘッドレス claude CLI の導入と動作確認
- [x] ローカルWeb版の最小プロトタイプ（server.py / index.html / start.bat）
- [x] UI改善: 作業フォルダ名表示・AI返信のMarkdown整形・トークン使用量表示
- [x] 作業フォルダの設定/切替（一覧を projects.json に保存・GUIで追加/切替/削除）
- [x] トークン使用量のグラフ化（コンテキスト使用量バー＋累計表示）
- [x] フォルダ追加をネイティブ選択ダイアログ化（パス手入力を廃止）
- [x] メッセージ送信ごとのモデル選択（Opus/Sonnet/Haiku）
- [x] 編集モードの安全な許可（トグルON時のみEdit/Writeを作業フォルダ内に限定。PreToolUseフック app/edit_guard.py で強制。Bashは無効・フォルダ外書込は拒否。ブロック時はUIに警告表示）
- [x] AI応答のリアルタイム表示（stream-jsonでCLI出力をndjsonとして逐次配信、UIで文字が生成されていく様子を表示）
- [x] 独立ウィンドウ化（起動時にEdge/Chromeの専用プロファイル＋--appモードでタブ・アドレスバー無しの単独ウィンドウを開く。ブラウザタブに紛れない）
- [x] 実行（都度確認）モードの追加・デフォルト化（3モード: 助言のみ/編集/実行。実行モードはEdit/Writeをフォルダ内で自動許可しつつ、Bash（シェル・アプリ実行）を1コマンドずつUIで許可/拒否。フックがサーバー経由でユーザーに問い合わせて実行時にブロック）
- [x] 安全なシェル操作の自動許可（読み取り専用コマンドの許可リストを独立ファイル app/safe_commands.txt に外出し。都度確認モードで git log/status 等の読み取り専用git・head・echo などを、確認カードは表示しつつ自動許可。判定ロジック app/safe_shell.py はリダイレクト/コマンド置換/連結を弾き、迷えば手動確認にフォールバックする安全側設計）
- [x] 安全コマンド自動許可の拡充（`cd` を許可リストに追加／`find` は書込・実行系プリミティブ(-exec/-delete/-fprintf等)を含まない読み取り専用形のみ専用ガード _find_safe で自動許可／`2>/dev/null`・`2>&1`・`>NUL` 等の無害リダイレクトは _SAFE_REDIR_RE で除去してから判定。`cd X && git status` のような連結が自動許可されるように）
- [x] 会話履歴の永続化（記憶ログ方式・プロジェクトごと・サーバ再起動後も引き継ぎ）。フルログ復元はトークン浪費のため不採用。AIが「引き継ぐべき進行中状態がある時だけ」返信末尾に [[TODOCHAT_MEMORY]] ブロックを付与→サーバが抽出し .todochat/memory/ に保存（表示からは除去）。起動時の挨拶に TASKS.md と並べて注入。/clear（チャット入力）でセッション破棄＋記憶削除＋画面クリア。削除・変更前に必ず .todochat/trash/ へバックアップ（サーバは trash を一切読まない＝プロンプトに再投入されずトークン非消費）。.todochat/ は gitignore 済み
- [x] 手動スナップショット `/remember`（チャットに入力すると、AIの自己判断を待たずに今すぐ記憶ログへ保存を強制。既存セッションをresumeして記憶ブロックのみを要求し、通常の保存経路で .todochat/memory/ に書き込み。UIには保存確認と保存内容を表示。実機確認済み）
- [x] ウィンドウ終了時の自動記憶（トグルUI「💾終了時記憶」で有効化。終了ボタン押下時に snapshot_memory_blocking() → finalize_memory_if_enabled() がHaikuで記憶スナップショットを同期取得し保存。有効/無効は projects.json に auto_remember_enabled として保存され起動時に復元。実機確認済み）
- [x] 記憶ログの閲覧/手動編集UI（ヘッダの🧠ボタンからモーダルを開き、現在の記憶ログ（.todochat/memory/）を表示・直接編集して保存。保存前の旧内容は他の上書き/削除と同様に必ずtrashへバックアップ。本文を空にして保存するとノートを削除。実機確認済み）
